// cookie-shop Start
var pionnerSquare = {
  minCustomer= 17,
  maxCustomer= 88,
  avgCookieSale= 5.2
};
 pionnerSquare.customerPerHour=function() {
     return math.floor(math.random() this.minCustomer =< this.maxCustomer);
 }

 var portlandAirport = {
   minCustomer= 6,
   maxCustomer= 24,
   avgCookieSale= 1.2
 };

 portlandAirport.customerPerHour =function() {
    return math.floor(math.random() this.minCustomer =< this.maxCustomer);
 }

var washingtonSquare = {
   minCustomer= 11,
   maxCustomer= 38,
   avgCookieSale= 1.2
 };

 washingtonSquare.customerPerHour =function() {
      return math.floor(math.random() this.minCustomer =< this.maxCustomer);
 }

 var sellwood = {
   minCustomer= 20,
   maxCustomer= 48,
   avgCookieSale= 3.3
 };

 sellwood.customerPerHour =function() {
      return math.floor(math.random() this.minCustomer =< this.maxCustomer);
 }

 var pearlDistrict= {
   minCustomer= 3,
   maxCustomer= 24,
   avgCookieSale= 2.6
 };

 pearlDistrict.customerPerHour = function() {
        return math.floor(math.random() this.minCustomer =< this.maxCustomer);
 }

customerPerHour();
